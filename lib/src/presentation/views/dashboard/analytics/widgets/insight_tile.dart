import 'package:flutter/material.dart';

class InsightTile extends StatelessWidget {
  const InsightTile({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox();
  }
}
